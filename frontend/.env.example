# Frontend-only environment overrides.
# Copy this file to `.env.local` when you want the frontend to connect
# to a non-default backend address.
# Do not put API keys or backend secrets in this file.

VITE_API_BASE_URL=http://localhost:8080
VITE_WS_BASE_URL=ws://localhost:8080
